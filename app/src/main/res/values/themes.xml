<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyFriends" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyFriends" parent="Base.Theme.MyFriends" />
    <!-- res/values/themes.xml -->
    <style name="MyToolbarTheme" parent="ThemeOverlay.AppCompat.ActionBar">
        <!-- This sets the color of the menu icons (the three dots) to be black -->
        <item name="android:textColorSecondary">@android:color/black</item>

        <!-- ADD THIS LINE to move the popup menu down -->
        <item name="android:dropDownVerticalOffset">50dp</item>
    </style>
</resources>
